#series operation

INPUT:

import pandas as pd

data = ['Omkar','20','Male','10']
series = pd.Series(data, index=['Name', 'Age', 'Gender', 'Rating'])
print(series)

Process finished with exit code 0

#DataFrame Operation

import pandas as pd

data={
    'Name':['omkar','parth'],
    'Age':[20,80],
    'Colification':['Btec','Btec']
    }
om= pd.DataFrame(data)
print(om)
